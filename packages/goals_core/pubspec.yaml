name: goals_core
description: Core Library that would be shared by any app for displaying goals
version: 1.0.0

publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  hlc: ^1.0.3
  rxdart: ^0.28.0
  uuid: ^4.1.0
  goals_types: 
    path:
      ../goals_types
  collection: ^1.17.2
  intl: any
  logging: ^1.2.0
  sortedmap: ^0.5.4
  validated: ^2.0.0
  queue: ^3.4.0
  concurrent_queue: ^1.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
